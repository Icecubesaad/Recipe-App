import React from 'react';

const Responsive = (props) => {
    return (
        <div>
        <div style={{color:"red",fontSize:"0.8rem",textAlign:"center"}}>{props.Error}</div>
        </div>
    );
}

export default Responsive;
