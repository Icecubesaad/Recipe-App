import React, { useContext } from "react";

const SigninFail = () => {
  return (
    <div class="alert alert-warning" role="alert">
      Invalid credentials
    </div>
  );
};

export default SigninFail;
